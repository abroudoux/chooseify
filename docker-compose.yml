version : '3'
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "3000:5175"

  server:
    build: 
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "8080:5000"